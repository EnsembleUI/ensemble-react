View:
  header:
    title:
      Text:
        text: Social Login

  body:
    Column:
      children:
        - SignInWithGoogle:
        - Conditional:
            conditions:
              - if: ${ensemble.user.email}
                Text:
                  text: Social Login
