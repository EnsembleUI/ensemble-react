View:
  header:
    Text:
      text: Add groups
      styles:
        fontSize: 24
        fontWeight: 500
  onLoad:
    executeCode: console.log("I am in the ad groups")
  body:
    Column:
      children:
        - GridView:
            styles:
              horizontalTileCount: 1
            item-template:
              data: ensemble.storage.get("arrayOfCollections")
              name: user
              template:
                Row:
                  mainAxis: spaceBetween
                  crossAxis: center
                  gap: 30
                  borderWidth: "2px"
                  borderColor: black
                  borderRadius: "4px"
                  minWidth: "450px"
                  children:
                    - Avatar:
                        styles:
                          backgroundColor: "#D9D9D9"
                        name: ${user.fields.Title}
                    - Column:
                        children:
                          - Text:
                              text: ${user.fields.Title}
                              styles:
                                fontSize: 15
                                fontWeight: 500
                          - Text:
                              text: ${user.fields.sources}
                              styles:
                                fontSize: 13
                                fontWeight: 400
                    - Button:
                        label: ""
                        startingIcon:
                          id: icon_1
                          name: RadioButtonUnchecked
                        styles:
                          backgroundColor: transparent
                          borderColor: transparent
        - Row:
            mainAxis: end
            crossAxis: center
            styles:
              gap: 16
            children:
              - Text:
                  text: 0 selected
                  styles:
                    color: black
              - Button:
                  label: Add
                  submitForm: true
                  onTap:
                    showToast:
                      message: ${"Created new user " + groupName.value}
                  styles:
                    textColor: white
                    backgroundColor: black
                    color: white
                    padding: 24
