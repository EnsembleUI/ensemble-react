View:
  onLoad:
    executeCode: |
      ensemble.storage.set('productTitleName', 'test 123');

  body:
    Column:
      children:
        - Button:
            styles:
              className: ${ensemble.storage.get('zzz') + '-' + ensemble.storage.get('aaa')}
            label: ${productTitleName()}
            onTap:
              executeCode: |
                console.log(ensemble)

Global:
  scriptName: test.js

Import:
  - common
